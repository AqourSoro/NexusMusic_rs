import { Button, VerticalBox } from "std-widgets.slint";

export component MainWindow inherits Window 
{
    in property <int> text_count : 1;
    callback on_click <=> btn.clicked;

    VerticalBox 
    {
        Text 
        {
            text: "Hello Nexus Music!";
        }
        btn := Button 
        {
            text: "Nexus!";
        }
    }    
}